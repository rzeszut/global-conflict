sourceCompatibility = 1.7

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        compile "com.badlogicgames.gdx:gdx-tools:$gdxVersion"
    }
}

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'
sourceSets.main.java.srcDirs = [ "src/" ]


import com.badlogic.gdx.tools.imagepacker.ImagePacker
class ImageAtlasCreateTask extends DefaultTask {
    def from
    def to
    def packName
    def images

    @TaskAction
    def pack() {
        def packer = new ImagePacker()
    }
}

//task generateAmericaAtlas(type: TextureSetupTask) {
//    from = "android/assets/image/america"
//    to = "android/assets/image/gen"
//    packName = "america.atlas"
//}
//processResources.dependsOn generateAmericaAtlas

eclipse.project {
    name = appName + "-core"
}
